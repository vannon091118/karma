
# Project managed by LLM Middleware Runtime

This project uses `karma` as a persistent context layer. Available commands:
- `karma status --project karma` — show sync state
- `karma memory get <domain> [key]` — read project memory
- `karma skill list` — list available skills
- `karma dispatch '<request>' --project karma` — auto-select skills & build delegate tasks
